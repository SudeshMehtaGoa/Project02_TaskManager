import { RowHeighlighterDirective } from './row-heighlighter.directive';

describe('RowHeighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new RowHeighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
